sf transit alexa skill
======================
how to deploy
-------------
- run `make build.zip` to generate `build.zip`. Upload this to AWS lambda.
- check [alexa skill configuration](https://developer.amazon.com/edw/)
  - check that ARNs match
